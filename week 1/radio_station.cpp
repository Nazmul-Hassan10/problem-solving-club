#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    pair<string, int> pr;
    map<string, int> mp;

    for (int i = 0; i < n + m; i++)
    {
        string s;
        getline(cin, s);
        int a, b, c;
    }
    
    return 0;
}